#include "stdio.h"
#include "string.h"
int main(){
	int fine,len,count;
	char numio[101];
	scanf("%d\n%s",&fine,numio);
	len = strlen(numio)-1;
	for(int i=0;i<len;i++){
		if(numio[i]=='1'){}
	}
	return 0;
}