#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main () {
   char num[6],it;
   int len;
   scanf("%s", num);
   len = strlen(num)-1;
   int num_ls[len];
   for(int i=0;i<len;i++){
   }
   // printf("%d\n", atoi(num));
   return 0;
}